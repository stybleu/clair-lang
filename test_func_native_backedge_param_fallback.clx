fn widen_param_seed(seed: int, n: int) -> float:
    total = seed
    i = 0
    while i < n:
        total = total + 0.5
        i = i + 1
    return total

print(widen_param_seed(2, 2))
print(widen_param_seed(9007199254740993, 0))
