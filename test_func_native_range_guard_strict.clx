fn range_guard_strict(n: int) -> float:
    value = 0
    if n > -100 and n < 100:
        value = n
    else:
        return 0.0
    i = 0
    while i < 1:
        value = value + 0.5
        i = i + 1
    return value

print(range_guard_strict(99))
print(range_guard_strict(100))
