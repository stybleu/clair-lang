fn range_assign_add(n: int) -> float:
    if n < 0 or n > 100:
        return 0.0
    else:
        value = n + 10
    while value < 12:
        value = value + 0.5
    return value

print(range_assign_add(0))
print(range_assign_add(10))
print(range_assign_add(101))
