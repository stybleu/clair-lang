fn range_assign_sub(n: int) -> float:
    if n < -100 or n > 100:
        return 0.0
    else:
        value = n - 10
    i = 0
    while i < 1:
        value = value + 0.5
        i = i + 1
    return value

print(range_assign_sub(-100))
print(range_assign_sub(100))
print(range_assign_sub(101))
