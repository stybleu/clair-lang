fn range_assign_mul_fallback(n: int) -> float:
    if n < 0 or n > 100:
        return 0.0
    else:
        value = n * 2
    i = 0
    while i < 1:
        value = value + 0.5
        i = i + 1
    return value

print(range_assign_mul_fallback(50))
print(range_assign_mul_fallback(101))
